# Deploy

## Command

```shell
sudo apt update
```

```shell
wget https://raw.githubusercontent.com/barkisnet/barkisnet-deploy/master/devnet/deploy.sh -O deploy.sh && sh deploy.sh [moniker] [miniGasPrice]
```

Example command:

```shell
wget https://raw.githubusercontent.com/barkisnet/barkisnet-deploy/master/devnet/deploy.sh -O deploy.sh && sh deploy.sh myBarkisNode 0.01ubarkis
```
